import type { Theme, Language } from '@/types/preferences'

export const DEFAULT_THEME: Theme = 'system'
export const DEFAULT_LANGUAGE: Language = 'en'

export const SUPPORTED_LANGUAGES: Language[] = ['en', 'uz']
